{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*.{js,css,md}": "prettier --write --ignore-unknown --ignore-path.huskyignore"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "nuxt": "^3.7.1",
    "prettier": "^3.1.1"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.11",
    "axios": "^1.5.0",
    "pinia": "^2.1.6",
    "sass": "^1.66.1"
  },
  "overrides": {
    "vue": "latest"
  }
}
